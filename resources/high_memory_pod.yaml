apiVersion: v1
kind: Pod
metadata:
  name: backend-cka06-arcg
  namespace: default
spec:
  containers:
  - name: memory-demo-ctr
    image: polinux/stress
    resources:
      requests:
        memory: "200Mi"
      limits:
        memory: "400Mi"
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "390M", "--vm-hang", "1"]
